fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy>=2.0.31
python-jose[cryptography]==3.3.0
passlib==1.7.4
python-multipart==0.0.6
pydantic>=2.5.0
pydantic-core>=2.14.0
pydantic-settings==2.1.0
requests==2.31.0
python-dotenv==1.0.0
asyncpg==0.31.0
psycopg2-binary==2.9.12
rapidfuzz>=3.6.0
metaphone>=0.6
pypdf>=5.0.0
PyMuPDF>=1.24.0
Pillow>=10.0.0
# Document OCR: pure pip install (onnxruntime, no torch, no system package or Docker build step)
# -- see ocr_service.py's module docstring and OCR_BENCHMARK.md for why this engine (RapidOCR)
# replaced docTR: docTR's torch dependency alone cost ~175MB RSS before any model even loaded,
# which combined with model weights put every real OCR call over Render's 512MB `starter` plan
# regardless of which docTR architecture was used -- confirmed by a live production OOM.
rapidocr-onnxruntime==1.2.3
